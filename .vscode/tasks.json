{
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "git push;ssh maxwell@192.168.1.3 'cd git/SQLparse; git pull; ./build; ./run'"
        }
    ]
}